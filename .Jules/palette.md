## 2024-03-26 - Add ARIA labels to Icon-only Buttons
**Learning:** Icon-only interactive elements in React components often rely on `title` for tooltips, but without `aria-label`, screen reader users miss crucial context.
**Action:** Next time, always ensure any button with only an SVG or icon has a descriptive `aria-label`.
