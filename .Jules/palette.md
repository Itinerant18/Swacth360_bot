## 2026-04-20 - Add ARIA label to Send button\n**Learning:** The send button in `ChatInputBar.tsx` had icons (spinner/paper plane) but lacked an `aria-label` and `title`, making it inaccessible to screen readers and lacking a hover tooltip.\n**Action:** Add `aria-label="Send message"` and `title="Send message"` to icon-only buttons to ensure they are accessible and intuitive for all users.
