## 2024-05-14 - Icon-only Button Accessibility
**Learning:** Adding only a `title` attribute to icon-only buttons provides a tooltip on hover but is insufficient for screen readers; both `title` and `aria-label` are required.
**Action:** When creating or auditing icon-only buttons, always ensure both `title` and `aria-label` are present for comprehensive accessibility support.
